Voice-Search-Program a speech recognizer that recognizes speech and then queries the recognized speech and automatically opens web browser to display the results. Default Search engine used here is Google.

Built using Python 3

Install dependencies :- pip install SpeechReocgnition 
                        pip install pyaudio
